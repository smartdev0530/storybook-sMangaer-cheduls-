import Duration from "./Duration";
export default {
  title: "Components/Input Forms/Duration",
  component: Duration,
};

const Template = (args) => <Duration {...args} />;

export const Primary = Template.bind({});
Primary.args = {
  
};
